name: Tractor
options:
  deploymentTarget:
    macOS: "15.0"
  bundleIdPrefix: com.jacobgroundwater

configFiles:
  Debug: Local.xcconfig
  Release: Local.xcconfig

settings:
  configs:
    Release:
      ENABLE_HARDENED_RUNTIME: YES
      CODE_SIGN_STYLE: Automatic
      OTHER_CODE_SIGN_FLAGS: --timestamp

packages:
  swift-argument-parser:
    url: https://github.com/apple/swift-argument-parser
    from: "1.3.0"
  swift-certificates:
    url: https://github.com/apple/swift-certificates
    from: "1.0.0"
  swift-crypto:
    url: https://github.com/apple/swift-crypto
    from: "3.0.0"

targets:
  TractorNE:
    type: bundle
    platform: macOS
    attributes:
      ProvisioningStyle: Automatic
    sources:
      - path: Sources/TractorNE
    dependencies:
      - package: swift-certificates
        product: X509
      - package: swift-crypto
        product: Crypto
      - package: swift-crypto
        product: _CryptoExtras
    entitlements:
      path: Sources/TractorNE/TractorNE.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.network.client: true
        com.apple.security.network.server: true
        com.apple.developer.networking.networkextension:
          - app-proxy-provider
        com.apple.security.application-groups:
          - group.com.jacobgroundwater.Tractor
    info:
      path: Sources/TractorNE/Info.plist
      properties:
        CFBundlePackageType: SYSX
        CFBundleShortVersionString: "0.8.0"
        CFBundleVersion: "166"
        LSMinimumSystemVersion: "15.0"
        NSSystemExtensionUsageDescription: "Tractor uses a network extension to monitor and intercept network traffic."
        NetworkExtension:
          NEProviderClasses:
            com.apple.networkextension.app-proxy: "$(PRODUCT_MODULE_NAME).TransparentProxy"
          NEMachServiceName: group.com.jacobgroundwater.Tractor
    settings:
      base:
        PRODUCT_NAME: com.jacobgroundwater.Tractor.NE
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobgroundwater.Tractor.NE
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: Sources/TractorNE/TractorNE.entitlements
        INFOPLIST_FILE: Sources/TractorNE/Info.plist
        SKIP_INSTALL: "YES"
        WRAPPER_EXTENSION: systemextension
        MACH_O_TYPE: mh_execute
      configs:
        Debug:
          PROVISIONING_PROFILE_SPECIFIER: ""

  TractorES:
    type: bundle
    platform: macOS
    attributes:
      ProvisioningStyle: Automatic
    sources:
      - path: Sources/TractorES
    entitlements:
      path: Sources/TractorES/TractorES.entitlements
      properties:
        com.apple.developer.endpoint-security.client: true
        com.apple.security.application-groups:
          - group.com.jacobgroundwater.Tractor
    info:
      path: Sources/TractorES/Info.plist
      properties:
        CFBundlePackageType: SYSX
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        LSMinimumSystemVersion: "15.0"
        NSSystemExtensionUsageDescription: "Tractor uses an Endpoint Security extension to monitor process activity for the process tree you trace."
    settings:
      base:
        PRODUCT_NAME: com.jacobgroundwater.Tractor.ES
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobgroundwater.Tractor.ES
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: Sources/TractorES/TractorES.entitlements
        INFOPLIST_FILE: Sources/TractorES/Info.plist
        SKIP_INSTALL: "YES"
        WRAPPER_EXTENSION: systemextension
        MACH_O_TYPE: mh_execute
        OTHER_LDFLAGS:
          - "-lEndpointSecurity"
          - "-lbsm"
      configs:
        Debug:
          PROVISIONING_PROFILE_SPECIFIER: ""

  TractorApp:
    type: application
    platform: macOS
    attributes:
      ProvisioningStyle: Automatic
    sources:
      - path: Sources/Tractor
        excludes:
          - "*.png"
      - path: icon/Tractor.icns
        buildPhase: resources
    dependencies:
      - package: swift-argument-parser
        product: ArgumentParser
      - target: TractorNE
        embed: false
      - target: TractorES
        embed: false
    postBuildScripts:
      - script: |
          set -e
          SYSEXT_DST="${TARGET_BUILD_DIR}/${WRAPPER_NAME}/Contents/Library/SystemExtensions"
          mkdir -p "${SYSEXT_DST}"
          for SYSEXT_NAME in com.jacobgroundwater.Tractor.NE.systemextension com.jacobgroundwater.Tractor.ES.systemextension; do
            if [ -d "${TARGET_BUILD_DIR}/${SYSEXT_NAME}" ]; then
              SYSEXT_SRC="${TARGET_BUILD_DIR}/${SYSEXT_NAME}"
            else
              SYSEXT_SRC="$(find "${OBJROOT}" "${SYMROOT}" -type d -name "${SYSEXT_NAME}" -not -path "*/${WRAPPER_NAME}/*" 2>/dev/null | head -1)"
            fi
            if [ -z "${SYSEXT_SRC}" ] || [ ! -d "${SYSEXT_SRC}" ]; then
              echo "error: could not locate built ${SYSEXT_NAME}"; exit 1
            fi
            ditto "${SYSEXT_SRC}" "${SYSEXT_DST}/${SYSEXT_NAME}"
          done
          # Debug: ad-hoc resign so the outer app's seal covers both embedded sysexts.
          # Release: skip — `xcodebuild -exportArchive` re-signs everything later.
          if [ "${CONFIGURATION}" != "Release" ]; then
            /usr/bin/codesign --force \
              --sign - \
              --entitlements "${SRCROOT}/Sources/Tractor/TractorApp.entitlements" \
              --timestamp=none \
              "${TARGET_BUILD_DIR}/${WRAPPER_NAME}"
          fi
        name: "Embed System Extensions"
    entitlements:
      path: Sources/Tractor/TractorApp.entitlements
      properties:
        com.apple.developer.system-extension.install: true
        com.apple.developer.networking.networkextension:
          - app-proxy-provider
        com.apple.security.application-groups:
          - group.com.jacobgroundwater.Tractor
    info:
      path: Sources/Tractor/Info.plist
      properties:
        CFBundleIdentifier: com.jacobgroundwater.Tractor
        CFBundleName: Tractor
        CFBundleDisplayName: Tractor
        CFBundleExecutable: Tractor
        CFBundleIconFile: Tractor
        CFBundlePackageType: APPL
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        LSMinimumSystemVersion: "15.0"
        LSUIElement: true
        NSHighResolutionCapable: true
        NSSystemExtensionUsageDescription: "Tractor uses a network extension to monitor and intercept network traffic for security analysis."
    settings:
      base:
        PRODUCT_NAME: Tractor
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: Sources/Tractor/TractorApp.entitlements
        SWIFT_OBJC_BRIDGING_HEADER: Sources/Tractor/BridgingHeader.h
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobgroundwater.Tractor
        INFOPLIST_FILE: Sources/Tractor/Info.plist
        OTHER_LDFLAGS:
          - "-lsqlite3"
          - "-framework"
          - "NetworkExtension"
          - "-framework"
          - "SystemExtensions"
